<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <meta charset="UTF-8">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(to right, #a1ffce, #faffd1);
      text-align: center;
      padding: 20px;
    }
    h1 {
      color: #333;
      margin-bottom: 25px;
    }
    .place {
      background-color: #fff;
      border-radius: 15px;
      padding: 15px;
      margin: 15px auto;
      width: 300px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.1);
      transition: transform 0.2s ease;
    }
    .place:hover {
      transform: scale(1.05);
    }
    .place img {
      width: 100%;
      height: 180px;
      border-radius: 10px;
      object-fit: cover;
    }
    .popular {
      color: #e91e63;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <h1><%= title %></h1>

  <% for (let i = 0; i < places.length; i++) { %>
    <div class="place">
      <img src="<%= places[i].image %>" alt="<%= places[i].name %>">
      <p><strong><%= places[i].name %></strong> — <%= places[i].country %></p>
      <% if (places[i].isPopular) { %>
        <p class="popular">✨🍃 Popular Destination!✨🍃</p>
      <% } %>
    </div>
  <% } %>

  <script>
    alert("Welcome to Beyond Boundaries!");
  </script>
</body>
</html>
